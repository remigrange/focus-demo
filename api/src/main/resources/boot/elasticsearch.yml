index :
       analysis :
             analyzer :
                    code :
                           tokenizer : keyword
                           filter : [standard]
                    text_fr :
                           tokenizer : standard
                           filter : [standard, lowercase]
                    sep_virgule :
                           tokenizer : sep_virgule
                           filter : [standard]
             tokenizer :
                    keyword :
                           type : keyword
                    sep_virgule :
                           type : pattern
                           pattern : '([,]+)'
                           
